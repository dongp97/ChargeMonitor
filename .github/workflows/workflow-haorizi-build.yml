name: HaoRiZi Build

on:
  push:
    branches: [ main, master ]
    paths:
      - 'haorizi/**'
      # 必须把自己也列进去，否则「只改 workflow」的推送不会触发构建
      - '.github/workflows/workflow-haorizi-build.yml'
      - '.github/workflows/haorizi-build.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Resolve SDK path and accept licenses
        run: |
          SDK="${ANDROID_HOME:-${ANDROID_SDK_ROOT:-$HOME/Android/Sdk}}"
          echo "ANDROID_HOME=$SDK" >> $GITHUB_ENV
          echo "ANDROID_SDK_ROOT=$SDK" >> $GITHUB_ENV
          mkdir -p "$SDK/licenses"
          printf '\n24333f8a63b6825ea9c5514f83c2829b004d1fee' > "$SDK/licenses/android-sdk-license"
          printf '\n84831b9409646a918e30573bab4c9c91346d8abd' > "$SDK/licenses/android-sdk-preview-license"
          echo "SDK path: $SDK"

      - name: Install SDK packages
        run: |
          SM="$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager"
          if [ -x "$SM" ]; then
            yes | "$SM" --install "platforms;android-35" "build-tools;35.0.0" || true
          else
            echo "sdkmanager 不存在，交给 AGP 自动下载（license 已写入）"
          fi

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-haorizi-${{ hashFiles('haorizi/**/*.gradle*', 'haorizi/gradle.properties') }}
          restore-keys: |
            ${{ runner.os }}-haorizi-

      - name: Install Gradle 8.9
        run: |
          wget -q https://services.gradle.org/distributions/gradle-8.9-bin.zip
          sudo unzip -q gradle-8.9-bin.zip -d /opt
          sudo ln -sfn /opt/gradle-8.9/bin/gradle /usr/local/bin/gradle
          gradle --version

      - name: Build Debug APK
        run: gradle -p haorizi --no-daemon --stacktrace assembleDebug

      - name: Locate APK
        run: |
          echo "=== apk outputs tree ==="
          find haorizi/app/build/outputs -type f -name '*.apk' -print || true
          echo "=== renaming ==="
          APK=$(find haorizi/app/build/outputs -type f -name '*.apk' | head -n 1)
          if [ -z "$APK" ]; then
            echo "::error::No APK produced by assembleDebug"
            exit 1
          fi
          # 产物名带版本号：同一次改动前后很容易都叫 v1.0，装上分不清是哪一版
          VN=$(grep -oP 'versionName\s*=\s*"\K[^"]+' haorizi/app/build.gradle.kts | head -n 1)
          VN=${VN:-unknown}
          echo "versionName=$VN"
          mkdir -p dist
          cp "$APK" "dist/HaoRiZi-v${VN}-debug.apk"
          ls -lh dist/

      - name: Upload APKs
        uses: actions/upload-artifact@v4
        with:
          name: haorizi-apk
          path: dist/*.apk
          if-no-files-found: error
          retention-days: 30
